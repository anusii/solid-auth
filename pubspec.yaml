name: solid_auth
description: Authenticate to a Solid POD server using Solid-OIDC with certified oidc.
version: 1.0.8
homepage: https://github.com/anusii/solid_auth
repository: https://github.com/anusii/solid_auth

environment:
  sdk: '>=3.11.0 <4.0.0'
  flutter: '>=3.41.0'

dependencies:

  # Crypto package
  crypto: ^3.0.7

  # Keystore-backed storage for the OIDC tokens, passed to OidcDefaultStore
  flutter_secure_storage: ^10.3.1

  # Core OIDC package (OpenID certified) replacing the forked openid_client
  oidc: ^4.0.0
  oidc_core: ^3.0.0
  oidc_default_store: ^1.0.0

  # JWT handling — kept for DPoP proof generation
  dart_jsonwebtoken: ^3.2.0

  # RSA key pair generation for DPoP
  fast_rsa: ^3.8.1

  # HTTP
  http: ^1.3.0

  # Utilities
  logging: ^1.3.0
  uuid: ^4.5.1

dev_dependencies:
  flutter:
    sdk: flutter
  flutter_lints: ^6.0.0
  url_launcher: any
  # 20260904 gjw The example is declared here so the dependency
  # checker sees the packages that only example/ imports (such as
  # url_launcher) as used.  Keep the name in step with
  # example/pubspec.yaml.
  solidautheg:
    path: example

# Running platforms
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

# fast_rsa doesn't declare its own web assets in pubspec.yaml, so they
# must be declared here or they won't be present in the web build output.
flutter:
  assets:
    - packages/fast_rsa/web/assets/worker.js
    - packages/fast_rsa/web/assets/wasm_exec.js
    - packages/fast_rsa/web/assets/rsa.wasm
